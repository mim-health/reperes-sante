name: assistant v2 closed corpus

on:
  push:
    branches:
      - feat/assistant-ia-v2-closed-corpus
    paths:
      - '.github/workflows/assistant-v2-corpus.yml'
      - 'scripts/build-assistant-v2-corpus.js'
      - 'scripts/test-assistant-v2-retrieval.js'
      - 'scripts/calibrate-assistant-v2-retrieval.js'
      - 'scripts/build-assistant-v2-embeddings.js'
      - 'scripts/benchmark-assistant-v2-semantic.js'
      - 'scripts/test-assistant-v2-semantic-module.js'
      - 'scripts/benchmark-assistant-v2-synthesis.js'
      - 'assistant-v2/retrieval.js'
      - 'assistant-v2/semantic-retrieval.js'
      - 'assistant-v2/contract.js'
      - 'assistant-v2/synthesis.js'
      - 'assistant-v2/retrieval.test-cases.json'
      - 'assistant-v2/retrieval.calibration-cases.json'
      - 'assistant-v2/synthesis.safety-cases.json'
      - 'corpus-manifest.js'
      - 'corpus-canonicalizer.js'
      - 'maca-category-access.js'
      - 'qa-data*.js'
      - 'evidence-model.js'
      - 'audit-overrides.js'
      - 'backlog-*.js'
      - 'maca-extra*.js'
      - 'maca-daily-archive.js'
      - 'structured-backlog-compat.js'
      - 'migration-*.js'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-assistant-v2-corpus:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact commit
        uses: actions/checkout@v4

      - name: Build isolated Assistant V2 corpus
        run: node scripts/build-assistant-v2-corpus.js

      - name: Test local retrieval baseline
        run: node scripts/test-assistant-v2-retrieval.js

      - name: Calibrate retrieval on mixed 2B cases
        run: node scripts/calibrate-assistant-v2-retrieval.js

      - name: Test semantic module without API
        run: node scripts/test-assistant-v2-semantic-module.js

      - name: Build embeddings and benchmark semantic retrieval when API key exists
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          MACA_EMBEDDING_MODEL: text-embedding-3-small
          MACA_EMBEDDING_DIMENSIONS: '512'
        run: |
          if [ -z "$OPENAI_API_KEY" ]; then
            echo "OPENAI_API_KEY absent: étape 3 live non exécutée, aucun coût API."
            exit 0
          fi
          node scripts/build-assistant-v2-embeddings.js
          node scripts/benchmark-assistant-v2-semantic.js

      - name: Run closed-corpus synthesis benchmark
        id: synthesis_benchmark
        continue-on-error: true
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          MACA_SYNTHESIS_MODEL: gpt-5.6-terra
          MACA_SYNTHESIS_TOP_K: '5'
          MACA_SYNTHESIS_MIN_SIMILARITY: '0.30'
          MACA_SYNTHESIS_CONCURRENCY: '3'
        run: |
          if [ -z "$OPENAI_API_KEY" ]; then
            echo "OPENAI_API_KEY absent: étape 4 live non exécutée."
            exit 1
          fi
          node scripts/benchmark-assistant-v2-synthesis.js

      - name: Show structural report
        run: cat assistant-v2/corpus.report.json

      - name: Show retrieval benchmark summary
        run: node -e "const r=require('./assistant-v2/retrieval.report.json'); console.log(JSON.stringify({ok:r.ok,testCount:r.testCount,passed:r.passed,failed:r.failed,passRate:r.passRate},null,2))"

      - name: Show 2B calibration summary
        run: node -e "const r=require('./assistant-v2/retrieval.calibration.report.json'); console.log(JSON.stringify({caseCount:r.caseCount,classCounts:r.classCounts,retrieval:r.retrieval,scoreSeparation:r.scoreSeparation,suggestedBaselineRule:r.suggestedBaselineRule},null,2))"

      - name: Show semantic benchmark summary when available
        run: |
          if [ -f assistant-v2/semantic.report.json ]; then
            node -e "const r=require('./assistant-v2/semantic.report.json'); console.log(JSON.stringify({embedding:r.embedding,baselineLexical:r.baselineLexical,pureSemantic:r.pureSemantic,hybrid:r.hybrid,semanticThreshold:r.semanticThresholdCalibration.suggestedStrict},null,2))"
          else
            echo "Semantic report absent: live API benchmark not run."
          fi

      - name: Show synthesis benchmark summary when available
        if: always()
        run: |
          if [ -f assistant-v2/synthesis.report.json ]; then
            node -e "const r=require('./assistant-v2/synthesis.report.json'); console.log(JSON.stringify({ok:r.ok,retrieval:r.retrieval,synthesis:r.synthesis,caseCount:r.caseCount,passed:r.passed,failed:r.failed,passRate:r.passRate,contractValid:r.contractValid,hardGateAbstains:r.hardGateAbstains,liveModelCalls:r.liveModelCalls,byClass:r.byClass,usage:r.usage},null,2))"
          else
            echo "Synthesis report absent."
          fi

      - name: Publish generated V2 artifacts on prototype branch only
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assistant-v2/corpus.json assistant-v2/corpus.report.json assistant-v2/retrieval.report.json assistant-v2/retrieval.calibration.report.json
          if [ -f assistant-v2/embeddings.index.json ]; then git add assistant-v2/embeddings.index.json; fi
          if [ -f assistant-v2/semantic.report.json ]; then git add assistant-v2/semantic.report.json; fi
          if [ -f assistant-v2/synthesis.report.json ]; then git add assistant-v2/synthesis.report.json; fi
          if git diff --cached --quiet; then
            echo "Assistant V2 artifacts unchanged"
          else
            git commit -m "build(assistant-v2): publier rapports retrieval et synthèse"
            git pull --rebase origin feat/assistant-ia-v2-closed-corpus
            git push origin HEAD:feat/assistant-ia-v2-closed-corpus
          fi

      - name: Enforce synthesis benchmark result
        if: always()
        run: |
          if [ ! -f assistant-v2/synthesis.report.json ]; then
            echo "Rapport synthèse absent"
            exit 1
          fi
          node -e "const r=require('./assistant-v2/synthesis.report.json'); if(!r.ok){console.error('Étape 4 non verte:',r.failed,'échec(s),',r.contractValid,'contrats valides sur',r.caseCount); process.exit(1)}; console.log('Étape 4 verte:',r.passed+'/'+r.caseCount)"
