/* MACA Santé V1 — navigation publique simplifiée à 8 rubriques. */
(function(){
  const PUBLIC=['Santé au quotidien','Cœur & prévention','Médicaments','Santé des femmes & grossesse','Enfants & parents','Ados','Après 60 ans','Santé mentale'];
  const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
  function target(card){const old=(card.querySelector('.qa-category')?.textContent||'').trim();const text=norm(`${old} ${card.querySelector('h3')?.textContent||''}`);if(/sante mentale|depression|anxiet|angoisse|stress|psych|suicid|moral/.test(text))return'Santé mentale';if(/ados?|adolesc/.test(text))return'Ados';if(/apres 60|senior|vieill|memoire|chute/.test(text))return'Après 60 ans';if(/enfant|parent|bebe|nourrisson|pediatr/.test(text))return'Enfants & parents';if(/grossesse|femme|feminin|menopause|fertilit|contracept|gyneco|cmv/.test(text))return'Santé des femmes & grossesse';if(/medicament|paracetamol|ibuprofene|antibiot|traitement|rupture/.test(text))return'Médicaments';if(/coeur|circulation|cardio|tension|hypertension|cholesterol|veine|thromb|phleb|varice|tour de taille|prevention|bien-etre|sport|activite physique/.test(text))return'Cœur & prévention';return'Santé au quotidien';}
  function remapCards(){document.querySelectorAll('#qa-grid .qa-card').forEach(card=>{const cat=card.querySelector('.qa-category');if(cat)cat.textContent=target(card);});}
  function buildFilters(){const box=document.querySelector('#category-filters');if(!box)return;box.innerHTML=['Toutes',...PUBLIC].map(c=>`<button class="filter-chip" data-category="${c}">${c}</button>`).join('');}
  function filterVisible(category){remapCards();document.querySelectorAll('#qa-grid .qa-card').forEach(card=>{const c=card.querySelector('.qa-category')?.textContent.trim();card.style.display=(category==='Toutes'||c===category)?'':'none';});}
  function stabilize(){const style=document.createElement('style');style.textContent='@media(max-width:800px){.search-hub{contain:layout style}.search-box{position:relative;transform:translateZ(0)}.search-box input{font-size:16px!important;line-height:1.3}.filter-chips{min-height:57px;scrollbar-width:none}.filter-chips::-webkit-scrollbar{display:none}#qa-grid{overflow-anchor:none}.qa-card{contain:layout style}}';document.head.appendChild(style);}
  function init(){stabilize();const box=document.querySelector('#category-filters'),input=document.querySelector('#search-input');if(!box)return;buildFilters();let publicActive='Toutes';box.addEventListener('click',e=>{const b=e.target.closest('.filter-chip');if(!b||b.dataset.internalRefresh==='1')return;const wanted=b.dataset.category||'Toutes';if(wanted==='Toutes'){publicActive='Toutes';setTimeout(()=>{remapCards();box.querySelectorAll('.filter-chip').forEach(x=>x.classList.toggle('active',x.dataset.category==='Toutes'));},0);return;}e.preventDefault();e.stopImmediatePropagation();const all=box.querySelector('[data-category="Toutes"]');if(all){all.dataset.internalRefresh='1';all.dispatchEvent(new MouseEvent('click',{bubbles:true}));delete all.dataset.internalRefresh;}publicActive=wanted;setTimeout(()=>{box.querySelectorAll('.filter-chip').forEach(x=>x.classList.toggle('active',x.dataset.category===wanted));filterVisible(wanted);if(input){input.dataset.scope=wanted;input.placeholder=`Rechercher dans « ${wanted} »…`;}},20);},true);const grid=document.querySelector('#qa-grid');if(grid)new MutationObserver(()=>{remapCards();if(publicActive!=='Toutes')filterVisible(publicActive);}).observe(grid,{childList:true});remapCards();const all=box.querySelector('[data-category="Toutes"]');if(all)all.classList.add('active');}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();